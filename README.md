# COCOAnnotationManager
COCO format annotation file manager. It could merge multiple annotation .json files into one annotation file, and also could split an annotation file into multiple annotation files.
